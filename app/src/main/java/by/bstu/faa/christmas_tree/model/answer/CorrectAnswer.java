package by.bstu.faa.christmas_tree.model.answer;

import android.app.AlertDialog;
import android.app.Dialog;
import android.os.Bundle;
import android.view.View;
import android.widget.Button;
import android.widget.TextView;

import androidx.fragment.app.DialogFragment;

import by.bstu.faa.christmas_tree.R;
import by.bstu.faa.christmas_tree.model.UserInfo;

public class CorrectAnswer extends DialogFragment {

/*
    @Override
    public Dialog onCreateDialog(Bundle savedInstanceState) {

        AlertDialog.Builder correct_builder = new AlertDialog.Builder(getActivity());
        View answerView = getLayoutInflater().inflate(R.layout.correct_answer_dialog, null);
        correct_builder.setView(answerView);

        TextView congrats = answerView.findViewById(R.id.congrats_correct);
        Button close_btn = answerView.findViewById(R.id.close_btn);

        UserInfo currentUser = (UserInfo) getArguments().getSerializable("USER");

    }*/
}
